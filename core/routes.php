<?php

if ( ! defined( 'ABSPATH' ) ) exit; // Exit if accessed directly

// Can't get this to work yet.

/*Routes::map(':folder', function($params){
    if ($params['name'] == 'teste') {
    Routes::load('index.php');
    };
});*/